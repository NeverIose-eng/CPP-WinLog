# CPP-WinLog
Simple lightweight Windows logging helper. Supports optional headers  *   (time, PE file name, module name, function name) and output modules  *   (console print, file output, default behavior). Designed to be small and  *   easy to integrate in native Windows C++ projects.
